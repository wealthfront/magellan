language: android

android:
  components:
    - tools
    - platform-tools
    - build-tools-27.0.2
    - android-27
    - extra

jdk:
  - oraclejdk8

script:
  - ./gradlew clean build

cache:
  directories:
    - $HOME/.gradle
